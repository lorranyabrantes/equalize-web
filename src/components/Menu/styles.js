import { SPACING } from "../../services/styles";

const styles = {
  container: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    padding: 0
  },
  item: {
    padding: SPACING.xsmall
  }
};

export default styles;
