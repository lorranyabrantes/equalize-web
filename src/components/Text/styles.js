import { COLORS, FONT_FAMILY, FONT_SIZES } from "../../services/styles";

const styles = {
  text: {
    color: COLORS.NIGHT_RIDER,
    fontFamily: FONT_FAMILY.roboto.regular,
    fontSize: FONT_SIZES.default,
  },
};

export default styles;
